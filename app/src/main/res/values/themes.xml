<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Bare activity theme; all real UI styling lives in the Compose theme, which covers the window
      before any of this is seen. Parented to AppCompat rather than the platform's Material because
      MainActivity is an AppCompatActivity — see the appcompat dependency and the language setting.
    -->
    <style name="Theme.TitleTrack" parent="Theme.AppCompat.NoActionBar" />
</resources>
